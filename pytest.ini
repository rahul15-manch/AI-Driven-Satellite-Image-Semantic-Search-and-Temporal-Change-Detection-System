[pytest]
markers =
    integration: Integration test across components or milestones.
    dataset: Tests requiring the real RSICD or LEVIR-CD datasets.
    cpu: Tests executed under strict CPU-only constraints.
filterwarnings =
    ignore::DeprecationWarning
